// Site-specific template renderers can live here in downstream websites.
// The skeleton ships with no active overrides; wire overrides in site/templates.ts
// only when a site intentionally needs a custom renderer for an existing template.
